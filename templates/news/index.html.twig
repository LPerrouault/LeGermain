{% extends 'base.html.twig' %}

{% block title %}Actualités - Picassouille{% endblock %}

{% block body %}
    <div class="container">
        <h2>Page des NEWS</h2>
        <div class="row">
            <div class="col-2">
                {% include 'news/_filter.html.twig'  %}
            </div>
            <div class="col-10">
                <section>
                    <div class="mosaic">
                        {% for article in articles %}
                            {% include 'news/_card.html.twig' %}
                        {% endfor %}
                    </div>
                </section>
                <div class="mt-4">
                    {{ knp_pagination_render(articles) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
