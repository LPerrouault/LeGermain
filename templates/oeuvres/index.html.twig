{% extends 'base.html.twig' %}

{% block title %}Oeuvres - Picassouille{% endblock %}

{% block body %}
    <h2>Oeuvres</h2>
    <div class="container">
        <div class="row">
            <div class="col-md-3">{% include 'oeuvres/_filter.html.twig'  %}</div>
            <div class="col-md-9">
                <div class="grid">
                    {% for oeuvres in oeuvres %}
                        {% include 'oeuvres/_card.html.twig' %}
                    {% endfor %}
                </div>
                <div class="mt-4">
                    {{ knp_pagination_render(oeuvres) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
